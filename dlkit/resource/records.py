from ..osid import records as osid_records


class ResourceRecord(osid_records.OsidRecord):
    """A record for a ``Resource``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceQueryRecord(osid_records.OsidRecord):
    """A record for a ``ResourceQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ResourceQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceFormRecord(osid_records.OsidRecord):
    """A record for a ``ResourceForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ResourceSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceSearchRecord(osid_records.OsidRecord):
    """A record for a ``ResourceSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ResourceSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationship``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipQueryRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipFormRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipSearchRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceRelationshipSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ResourceRelationshipSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinRecord(osid_records.OsidRecord):
    """A record for a ``Bin``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinQueryRecord(osid_records.OsidRecord):
    """A record for a ``BinQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``BinQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinFormRecord(osid_records.OsidRecord):
    """A record for a ``BinForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``BinSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinSearchRecord(osid_records.OsidRecord):
    """A record for a ``BinSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BinSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``BinSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



