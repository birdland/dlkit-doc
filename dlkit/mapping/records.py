from ..osid import records as osid_records


class LocationRecord(osid_records.OsidRecord):
    """A record for a ``Location``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationQueryRecord(osid_records.OsidRecord):
    """A record for a ``LocationQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``LocationInspectorQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationFormRecord(osid_records.OsidRecord):
    """A record for a ``LocationForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``LocationSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationSearchRecord(osid_records.OsidRecord):
    """A record for a ``LocationSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LocationSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``LocationSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapRecord(osid_records.OsidRecord):
    """A record for a ``MapRecord``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapQueryRecord(osid_records.OsidRecord):
    """A record for a ``MapQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``MapQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapFormRecord(osid_records.OsidRecord):
    """A record for a ``MapForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``MapSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapSearchRecord(osid_records.OsidRecord):
    """A record for a ``MapSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class MapSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``MapSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResourceLocationRecord(osid_records.OsidRecord):
    """A record for a ``ResourceLocationRecord``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SpatialUnitRecord(osid_records.OsidRecord):
    """A record for a ``SpatialUnitRecord``.

    The methods specified by the record type are available through the
    underlying object.

    """



