from ..osid import records as osid_records


class EndpointRecord(osid_records.OsidRecord):
    """A record for an ``Endpoint``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RequestRecord(osid_records.OsidRecord):
    """A record for a ``Request``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResponseRecord(osid_records.OsidRecord):
    """A record for a ``Response``.

    The methods specified by the record type are available through the
    underlying object.

    """



