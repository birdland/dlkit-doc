from ..osid import records as osid_records


class AuthenticationRecord(osid_records.OsidRecord):
    """A record for an ``Authentication``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthenticationInputRecord(osid_records.OsidRecord):
    """A record for an ``AuthenticationInput``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ChallengeRecord(osid_records.OsidRecord):
    """A record for a ``Challenge``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TrustRecord(osid_records.OsidRecord):
    """A record for a ``Trust``.

    The methods specified by the record type are available through the
    underlying object.

    """



