from ..osid import records as osid_records


class ProcessRecord(osid_records.OsidRecord):
    """A record for a ``Process``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessQueryRecord(osid_records.OsidRecord):
    """A record for a ``ProcessQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ProcessQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessFormRecord(osid_records.OsidRecord):
    """A record for a ``ProcessForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ProcessSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessSearchRecord(osid_records.OsidRecord):
    """A record for a ``ProcessSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProcessSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ProcessSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepRecord(osid_records.OsidRecord):
    """A record for a ``Step``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepQueryRecord(osid_records.OsidRecord):
    """A record for a ``StepQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``StepQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepFormRecord(osid_records.OsidRecord):
    """A record for a ``StepForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``StepSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepSearchRecord(osid_records.OsidRecord):
    """A record for a ``StepSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class StepSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``StepSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkRecord(osid_records.OsidRecord):
    """A record for a ``Work``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkQueryRecord(osid_records.OsidRecord):
    """A record for a ``WorkQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``WorkQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkFormRecord(osid_records.OsidRecord):
    """A record for a ``WorkForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``WorkSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkSearchRecord(osid_records.OsidRecord):
    """A record for a ``WorkSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``WorkSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class WorkflowEventRecord(osid_records.OsidRecord):
    """A record for a ``WorkflowEvent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeRecord(osid_records.OsidRecord):
    """A record for a ``OfficeRecord``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeQueryRecord(osid_records.OsidRecord):
    """A record for a ``OfficeQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``OfficeQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeFormRecord(osid_records.OsidRecord):
    """A record for a ``OfficeForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``OfficeSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeSearchRecord(osid_records.OsidRecord):
    """A record for a ``OfficeSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OfficeSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``OfficeSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



