from ..osid import records as osid_records


class AssetRecord(osid_records.OsidRecord):
    """A record for an ``Asset``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetQueryRecord(osid_records.OsidRecord):
    """A record for an ``AssetQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AssetQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetFormRecord(osid_records.OsidRecord):
    """A record for an ``AssetForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``AssetSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetSearchRecord(osid_records.OsidRecord):
    """A record for an ``AssetSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``AssetSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetContentRecord(osid_records.OsidRecord):
    """A record for an ``AssetContent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetContentQueryRecord(osid_records.OsidRecord):
    """A record for an ``AssetContentQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetContentQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AssetContentQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssetContentFormRecord(osid_records.OsidRecord):
    """A record for an ``AssetForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionRecord(osid_records.OsidRecord):
    """A record for a ``Composition``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionQueryRecord(osid_records.OsidRecord):
    """A record for a ``CompositionQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``CompositionQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionFormRecord(osid_records.OsidRecord):
    """A record for a ``CompositionForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``CompositionSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionSearchRecord(osid_records.OsidRecord):
    """A record for a ``CompositionSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CompositionSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``CompositionSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositoryRecord(osid_records.OsidRecord):
    """A record for a ``Repository``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositoryQueryRecord(osid_records.OsidRecord):
    """A record for a ``RepositoryQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositoryQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``RepositoryQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositoryFormRecord(osid_records.OsidRecord):
    """A record for a ``RepositoryForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositorySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``RepositorySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositorySearchRecord(osid_records.OsidRecord):
    """A record for a ``RepositorySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RepositorySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``RepositorySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



