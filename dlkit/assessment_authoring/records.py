from ..osid import records as osid_records


class AssessmentPartRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPart``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartQueryRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartFormRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartSearchRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AssessmentPartSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``AssessmentPartSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRule``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleQueryRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleFormRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleSearchRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnabler``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerQueryRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnablerQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnablerQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnablerSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerSearchRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnablerSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SequenceRuleEnablerSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``SequenceRuleEnablerSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



