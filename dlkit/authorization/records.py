from ..osid import records as osid_records


class AuthorizationRecord(osid_records.OsidRecord):
    """A record for an ``Authorization`` The methods specified by the record type are available through the underlying object."""



class AuthorizationQueryRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationFormRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationSearchRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AuthorizationConditionRecord(osid_records.OsidRecord):
    """A record for an ``AuthorizationCondition``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionRecord(osid_records.OsidRecord):
    """A record for a ``Function``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionQueryRecord(osid_records.OsidRecord):
    """A record for a ``FunctionQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``FunctionQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionFormRecord(osid_records.OsidRecord):
    """A record for a ``FunctionForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``FunctionSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionSearchRecord(osid_records.OsidRecord):
    """A record for a ``FunctionSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FunctionSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``FunctionSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierRecord(osid_records.OsidRecord):
    """A record for a ``Qualifier``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierQueryRecord(osid_records.OsidRecord):
    """A record for a ``QualifierQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``QualifierQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierFormRecord(osid_records.OsidRecord):
    """A record for a ``QualifierForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``QualifierSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierSearchRecord(osid_records.OsidRecord):
    """A record for a ``QualifierSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class QualifierSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``QualifierSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultRecord(osid_records.OsidRecord):
    """A record for a ``Vault``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultQueryRecord(osid_records.OsidRecord):
    """A record for a ``VaultQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``VaultQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultFormRecord(osid_records.OsidRecord):
    """A record for a ``VaultForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``VaultSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultSearchRecord(osid_records.OsidRecord):
    """A record for a ``VaultSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class VaultSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``VaultSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



