from ..osid import records as osid_records


class CreditRecord(osid_records.OsidRecord):
    """A record for a ``Credit``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditQueryRecord(osid_records.OsidRecord):
    """A record for a ``CreditQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``CreditQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditFormRecord(osid_records.OsidRecord):
    """A record for a ``CreditForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``CreditSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditSearchRecord(osid_records.OsidRecord):
    """A record for a ``CreditSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CreditSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``CreditSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingRecord(osid_records.OsidRecord):
    """A record for a ``Billing``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingQueryRecord(osid_records.OsidRecord):
    """A record for a ``BillingQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``CreditQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingFormRecord(osid_records.OsidRecord):
    """A record for a ``BillingForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``BillingSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingSearchRecord(osid_records.OsidRecord):
    """A record for a ``BillingSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BillingSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``BillingSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



