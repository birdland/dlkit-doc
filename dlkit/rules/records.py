from ..osid import records as osid_records


class RuleRecord(osid_records.OsidRecord):
    """A record for a ``Rule``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleQueryRecord(osid_records.OsidRecord):
    """A record for a ``RuleQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``RuleQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleFormRecord(osid_records.OsidRecord):
    """A record for a ``RuleForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``RuleSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleSearchRecord(osid_records.OsidRecord):
    """A record for a ``RuleSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RuleSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``RuleSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineRecord(osid_records.OsidRecord):
    """A record for an ``Engine``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineQueryRecord(osid_records.OsidRecord):
    """A record for an ``EngineQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``EngineQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineFormRecord(osid_records.OsidRecord):
    """A record for an ``EngineForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``EngineSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineSearchRecord(osid_records.OsidRecord):
    """A record for a ``EngineSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EngineSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``EngineSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ConditionRecord(osid_records.OsidRecord):
    """A record for a ``Condition``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ResultRecord(osid_records.OsidRecord):
    """A record for a ``Result``.

    The methods specified by the record type are available through the
    underlying object.

    """



