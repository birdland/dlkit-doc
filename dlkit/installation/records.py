from ..osid import records as osid_records


class PackageRecord(osid_records.OsidRecord):
    """A record for a ``Package``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageQueryRecord(osid_records.OsidRecord):
    """A record for a ``PackageQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``PackageQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageFormRecord(osid_records.OsidRecord):
    """A record for a ``PackageForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``PackageSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageSearchRecord(osid_records.OsidRecord):
    """A record for a ``PackageSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PackageSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``PackageSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationContentRecord(osid_records.OsidRecord):
    """A record for an ``InstallationContent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationContentQueryRecord(osid_records.OsidRecord):
    """A record for an ``InstallationContentQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationContentQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``InstallationContentQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationContentFormRecord(osid_records.OsidRecord):
    """A record for an ``InstallationContentForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotRecord(osid_records.OsidRecord):
    """A record for a ``Depot``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotQueryRecord(osid_records.OsidRecord):
    """A record for a ``DepotQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``DepotQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotFormRecord(osid_records.OsidRecord):
    """A record for a ``DepotForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``DepotSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotSearchRecord(osid_records.OsidRecord):
    """A record for a ``DepotSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DepotSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``DepotSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationRecord(osid_records.OsidRecord):
    """A record for an ``Installation``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationQueryRecord(osid_records.OsidRecord):
    """A record for an ``InstallationQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``InstallationQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``InstallationSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationSearchRecord(osid_records.OsidRecord):
    """A record for an ``InstallationSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class InstallationSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``InstallationSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SiteRecord(osid_records.OsidRecord):
    """A record for a ``Site``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SiteQueryRecord(osid_records.OsidRecord):
    """A record for a ``SiteQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SiteQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``SiteQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SiteSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``SiteSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



