from ..osid import records as osid_records


class PostRecord(osid_records.OsidRecord):
    """A record for a ``Post``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostQueryRecord(osid_records.OsidRecord):
    """A record for a ``PostQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``PostQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostFormRecord(osid_records.OsidRecord):
    """A record for a ``PostForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``PostSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostSearchRecord(osid_records.OsidRecord):
    """A record for a ``PostSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class PostSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``PostSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplyRecord(osid_records.OsidRecord):
    """A record for a ``Reply``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplyQueryRecord(osid_records.OsidRecord):
    """A record for a ``ReplyQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplyQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ReplyQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplyFormRecord(osid_records.OsidRecord):
    """A record for a ``ReplyForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ReplySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplySearchRecord(osid_records.OsidRecord):
    """A record for a ``ReplySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ReplySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ReplySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumRecord(osid_records.OsidRecord):
    """A record for a ``Forum``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumQueryRecord(osid_records.OsidRecord):
    """A record for a ``ForumQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ForumQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumFormRecord(osid_records.OsidRecord):
    """A record for a ``ForumForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ForumSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumSearchRecord(osid_records.OsidRecord):
    """A record for a ``ForumSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ForumSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ForumSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



