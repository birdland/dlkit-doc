from ..osid import records as osid_records


class EventRecord(osid_records.OsidRecord):
    """A record for an ``Event``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventQueryRecord(osid_records.OsidRecord):
    """A record for an ``EventQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``EventQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventFormRecord(osid_records.OsidRecord):
    """A record for an ``EventForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``EventSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventSearchRecord(osid_records.OsidRecord):
    """A record for an ``EventSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class EventSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``EventSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEvent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventQueryRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventFormRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventSearchRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class RecurringEventSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``RecurringEventSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEvent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventQueryRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventFormRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventSearchRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class SupersedingEventSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``SupersedingEventSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEvent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventQueryRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventFormRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventSearchRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class OffsetEventSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``OffsetEventSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleRecord(osid_records.OsidRecord):
    """A record for a ``Schedule``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleQueryRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleFormRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSearchRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlot``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotQueryRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotFormRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotSearchRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ScheduleSlotSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ScheduleSlotSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriod``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodQueryRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodFormRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodSearchRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class TimePeriodSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``TimePeriodSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentRecord(osid_records.OsidRecord):
    """A record for a ``Commitment``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentQueryRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentFormRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentSearchRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CommitmentSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``CommitmentSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarRecord(osid_records.OsidRecord):
    """A record for a ``Calendar``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarQueryRecord(osid_records.OsidRecord):
    """A record for a ``CalendarQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``CalendarQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarFormRecord(osid_records.OsidRecord):
    """A record for a ``CalendarForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``CalendarSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarSearchRecord(osid_records.OsidRecord):
    """A record for a ``CalendarSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class CalendarSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``CalendarSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



