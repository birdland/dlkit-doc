from ..osid import records as osid_records


class ObjectiveRecord(osid_records.OsidRecord):
    """A record for an ``Objective``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveQueryRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveFormRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveSearchRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivityRecord(osid_records.OsidRecord):
    """A record for a ``Activity``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivityQueryRecord(osid_records.OsidRecord):
    """A record for an ``ActivityQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivityQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``ActivityQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivityFormRecord(osid_records.OsidRecord):
    """A record for a ``ActivityForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivitySearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``ActivitySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivitySearchRecord(osid_records.OsidRecord):
    """A record for an ``ActivitySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ActivitySearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``ActivitySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencyRecord(osid_records.OsidRecord):
    """A record for a ``Proficiency``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencyQueryRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencyQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencyQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencyQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencyFormRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencyForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencySearchRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ProficiencySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ProficiencySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankRecord(osid_records.OsidRecord):
    """A record for a ``ObjectiveBank``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankQueryRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveBankQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``ObjectiveBankQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankFormRecord(osid_records.OsidRecord):
    """A record for a ``ObjectiveBankForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``ObjectiveBankSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankSearchRecord(osid_records.OsidRecord):
    """A record for a ``ObjectiveBankSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class ObjectiveBankSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``ObjectiveBankSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



