from ..osid import records as osid_records


class LogEntryRecord(osid_records.OsidRecord):
    """A record for a ``LogEntry``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntryQueryRecord(osid_records.OsidRecord):
    """A record for a ``LoglEntryQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntryQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``LogEntryQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntryFormRecord(osid_records.OsidRecord):
    """A record for a ``LogEntryForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntrySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``LogEntrySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntrySearchRecord(osid_records.OsidRecord):
    """A record for a ``LogEntrySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogEntrySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``LogEntrySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogRecord(osid_records.OsidRecord):
    """A record for a ``Log``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogQueryRecord(osid_records.OsidRecord):
    """A record for a ``LogQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``LogQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogFormRecord(osid_records.OsidRecord):
    """A record for a ``LogForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``LogSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogSearchRecord(osid_records.OsidRecord):
    """A record for a ``LogSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class LogSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``LogSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



