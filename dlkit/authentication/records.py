
from ..osid import records as osid_records


class AgentRecord(osid_records.OsidRecord):
    """A record for an ``Agent``.

    The methods specified by the record type are available through the
    underlying object.

    """




class AgentQueryRecord(osid_records.OsidRecord):
    """A record for an ``AgentQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """




class AgentFormRecord(osid_records.OsidRecord):
    """A record for an ``AgentForm``.

    The methods specified by the record type are available through the
    underlying object.

    """




class AgentSearchRecord(osid_records.OsidRecord):
    """A record for an ``AgentSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """




