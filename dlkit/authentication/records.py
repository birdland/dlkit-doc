from ..osid import records as osid_records


class AgentRecord(osid_records.OsidRecord):
    """A record for an ``Agent``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentQueryRecord(osid_records.OsidRecord):
    """A record for an ``AgentQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AgentQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentFormRecord(osid_records.OsidRecord):
    """A record for an ``AgentForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentSearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``AgentSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentSearchRecord(osid_records.OsidRecord):
    """A record for an ``AgentSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgentSearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``AgentSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencyRecord(osid_records.OsidRecord):
    """A record for an ``Agency``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencyQueryRecord(osid_records.OsidRecord):
    """A record for an ``AgencyQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencyQueryInspectorRecord(osid_records.OsidRecord):
    """A record for an ``AgencyQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencyFormRecord(osid_records.OsidRecord):
    """A record for an ``AgencyForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencySearchOrderRecord(osid_records.OsidRecord):
    """A record for an ``AgencySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencySearchRecord(osid_records.OsidRecord):
    """A record for an ``AgencySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class AgencySearchResultsRecord(osid_records.OsidRecord):
    """A record for an ``AgencySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



