from ..osid import records as osid_records


class JournalEntryRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntry``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntryQueryRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntryQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntryQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntryQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntryFormRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntryForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntrySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntrySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntrySearchRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntrySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalEntrySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``JournalEntrySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchRecord(osid_records.OsidRecord):
    """A record for a ``Branch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchQueryRecord(osid_records.OsidRecord):
    """A record for a ``BranchQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``BranchQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchFormRecord(osid_records.OsidRecord):
    """A record for a ``BranchForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``BranchSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchSearchRecord(osid_records.OsidRecord):
    """A record for a ``BranchSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class BranchSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``BranchSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalRecord(osid_records.OsidRecord):
    """A record for a ``Journal``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalQueryRecord(osid_records.OsidRecord):
    """A record for a ``JournalQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``JournalQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalFormRecord(osid_records.OsidRecord):
    """A record for a ``JournalForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``JournalSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalSearchRecord(osid_records.OsidRecord):
    """A record for a ``JournalSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class JournalSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``JournalSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



