from ..osid import records as osid_records


class FileRecord(osid_records.OsidRecord):
    """A record for a ``File``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileQueryRecord(osid_records.OsidRecord):
    """A record for a ``FileQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``FileQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileFormRecord(osid_records.OsidRecord):
    """A record for a ``FileForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileSearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``FileSearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileSearchRecord(osid_records.OsidRecord):
    """A record for a ``FileSearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class FileSearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``FileSearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectoryRecord(osid_records.OsidRecord):
    """A record for a ``Directory``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectoryQueryRecord(osid_records.OsidRecord):
    """A record for a ``DirectoryQuery``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectoryQueryInspectorRecord(osid_records.OsidRecord):
    """A record for a ``DirectoryQueryInspector``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectoryFormRecord(osid_records.OsidRecord):
    """A record for a ``DirectoryForm``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectorySearchOrderRecord(osid_records.OsidRecord):
    """A record for a ``DirectorySearchOrder``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectorySearchRecord(osid_records.OsidRecord):
    """A record for a ``DirectorySearch``.

    The methods specified by the record type are available through the
    underlying object.

    """



class DirectorySearchResultsRecord(osid_records.OsidRecord):
    """A record for a ``DirectorySearchResults``.

    The methods specified by the record type are available through the
    underlying object.

    """



